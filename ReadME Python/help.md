# help

Lists the commands currently loaded by the bot.

Usage: `/help` or `-help`

This project disables the default discord.py help command so the bot can provide a custom help embed that matches the command files in `AllCMDS`.
