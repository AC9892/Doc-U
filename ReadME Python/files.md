# files

Explains file uploads and attachments.

Usage: `/files` or `-files`

Key points:
- Send files with `discord.File`.
- Read received uploads through `discord.Attachment`.
- Attachment APIs include URL, filename, size, and async save/read helpers.
- Discord applies upload limits based on server/account settings.
