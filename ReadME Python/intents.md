# intents

Explains Discord gateway intents.

Usage: `/intents` or `-intents`

Important details:
- Intents decide which gateway events the bot receives.
- `members`, `presences`, and `message_content` are privileged in many cases.
- Prefix commands need message content intent.
- Overbroad intents can increase memory usage.
