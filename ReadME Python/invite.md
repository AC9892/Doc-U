# invite

Generates an OAuth2 invite URL for the bot.

Usage: `/invite` or `-invite`

The generated link includes:
- `bot` scope
- `applications.commands` scope
- message/embed permissions
- basic moderation permissions

Review the permissions before inviting the bot to production servers.
