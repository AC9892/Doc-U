# components

Explains buttons, select menus, views, and modals.

Usage: `/components` or `-components`

Key points:
- `discord.ui.View` groups interactive items.
- Buttons and selects use callbacks.
- Modals collect structured text input.
- Persistent views need stable custom IDs and startup registration.
