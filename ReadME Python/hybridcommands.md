# hybridcommands

Explains hybrid commands.

Usage: `/hybridcommands` or `-hybridcommands`

Key points:
- `@commands.hybrid_command` exposes one callback as prefix and slash.
- Hybrid commands are useful for migration from prefix bots.
- Parameters must stay compatible with Discord slash command option rules.
