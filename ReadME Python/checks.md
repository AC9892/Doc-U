# checks

Explains command checks.

Usage: `/checks` or `-checks`

Common checks:
- `commands.is_owner`
- `commands.guild_only`
- `commands.dm_only`
- `commands.has_permissions`
- `commands.bot_has_permissions`
- `commands.cooldown`
- custom `commands.check`

Checks block command callbacks before they run.
