# docindex

Shows a paginated index of the expanded local discord.py documentation catalog.

Usage:
- `/docindex`
- `-docindex`

The command groups references into categories such as core API, models, channels, commands extension, application commands, UI, tasks, voice, and exceptions.

Slash command replies are ephemeral.
