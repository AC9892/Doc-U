# botinfo

Shows runtime information for the bot.

Usage: `/botinfo` or `-botinfo`

Includes:
- discord.py version
- cached guild count
- cached user count
- loaded command count
- loaded cogs
- uptime

This command is useful for confirming that the extension loader successfully imported the files in `AllCMDS`.
