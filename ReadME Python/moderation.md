# moderation

Explains moderation APIs.

Usage: `/moderation` or `-moderation`

Covered actions:
- kick
- ban
- timeout
- delete message
- purge messages
- edit channels or roles

Always pass clear `reason=` values so server audit logs are useful.
