# avatar

Shows a user's display avatar.

Usage:
- `/avatar`
- `/avatar member:@Someone`
- `-avatar @Someone`

The command uses `display_avatar`, which respects guild avatars when available and falls back to the user's global avatar.
