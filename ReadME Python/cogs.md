# cogs

Explains discord.py cogs.

Usage: `/cogs` or `-cogs`

Key points:
- Cogs subclass `commands.Cog`.
- Cogs group commands, listeners, checks, and state.
- Cogs are normally loaded through extension files.
- Useful hooks include `cog_load`, `cog_unload`, `cog_check`, and `cog_command_error`.
